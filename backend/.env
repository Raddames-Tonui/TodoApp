# THIS ARE DEFAULT VALUES AND CAN BE CHANGED

POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb
POSTGRES_PORT=5432
POSTGRES_HOST=database 
# NOTE
# Flask locally + PostgreSQL in Docker → POSTGRES_HOST=127.0.0.1
# Flask in Docker + PostgreSQL in Docker → POSTGRES_HOST=my_postgres