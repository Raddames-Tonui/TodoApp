#!/bin/bash
echo "Files in this directory"
ls -a


# Before running the script you may need to give it executable permission. 
# You can change the permissions by running   chmod +x script.sh 
# Now we can run the script using the following command.

# ./script.sh